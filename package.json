{
  "name": "openstarter",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "pnpm with-env turbo run dev",
    "build": "pnpm with-env turbo run build",
    "test": "pnpm with-env turbo run test",
    "test:coverage": "pnpm with-env turbo run test:coverage",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "check-types": "pnpm with-env turbo run check-types",
    "dev:web": "pnpm with-env turbo run -F web dev",
    "dev:desktop": "pnpm with-env turbo run -F desktop dev",
    "dev:extension": "pnpm with-env turbo run -F extension dev",
    "dev:mobile": "pnpm with-env turbo run -F mobile dev",
    "dev:mobile:ios": "pnpm with-env turbo run -F mobile ios",
    "dev:mobile:android": "pnpm with-env turbo run -F mobile android",
    "dev:mini-app": "pnpm with-env turbo run -F mini-app dev",
    "dev:cli": "pnpm with-env turbo run -F @openstarter/cli dev",
    "build:web": "pnpm with-env turbo run -F web build",
    "build:extension": "pnpm with-env turbo run -F extension build",
    "build:mobile": "pnpm with-env turbo run -F mobile build",
    "build:cli": "pnpm with-env turbo run -F @openstarter/cli build",
    "build:mini-app": "pnpm with-env turbo run -F mini-app build",
    "build:desktop": "pnpm with-env turbo run -F desktop build",
    "package:desktop": "pnpm with-env turbo run -F desktop package",
    "release:desktop": "pnpm with-env turbo run -F desktop release",
    "db:push": "pnpm with-env turbo run -F @openstarter/db db:push",
    "db:studio": "pnpm with-env turbo run -F @openstarter/db db:studio",
    "db:generate": "pnpm with-env turbo run -F @openstarter/db db:generate",
    "db:migrate": "pnpm with-env turbo run -F @openstarter/db db:migrate",
    "db:local": "pnpm with-env turbo run -F @openstarter/db db:local",
    "seed:admin": "pnpm with-env turbo run -F @openstarter/auth seed:admin",
    "with-env": "dotenv -c --"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "4.1.10",
    "dotenv-cli": "^11.0.0",
    "fast-check": "4.9.0",
    "jsdom": "29.1.1",
    "oxfmt": "^0.64.0",
    "oxlint": "^1.79.0",
    "turbo": "^2.9.16",
    "typescript": "catalog:",
    "vitest": "4.1.10"
  },
  "packageManager": "pnpm@10.25.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "msw",
      "sharp",
      "workerd",
      "electron",
      "@tarojs/binding",
      "@tarojs/cli",
      "core-js",
      "core-js-pure"
    ]
  }
}
